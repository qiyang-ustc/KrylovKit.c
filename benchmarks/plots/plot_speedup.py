#!/usr/bin/env python3
"""Old speedup plotting is disabled during benchmark reconstruction."""

raise SystemExit(
    "old speedup plotting is disabled; use a finalized Krylov kernel schema "
    "before plotting ratios"
)
